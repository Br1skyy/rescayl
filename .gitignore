# dependencies
/node_modules/
/.pnp
.pnp.js

# next.js
/.next/
/renderer/.next/
/renderer/out/
/renderer/tsconfig.tsbuildinfo

# tauri
/src-tauri/target/
/src-tauri/gen/
*.tsbuildinfo

# production
/dist/

# downloaded / bundled binaries and model weights.
# These are fetched at build time (see update_upscayl_ncnn_binaries.sh) or
# downloaded at runtime from the marketplace, so they are not stored in git.
/resources/models/
/resources/linux/
/resources/mac/
/resources/win/
/resources/bin/

# misc
.DS_Store
Thumbs.db
*.pem
*.p12

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# editors / os
.vscode/
.idea/
*.swp